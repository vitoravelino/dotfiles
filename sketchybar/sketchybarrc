# This is a demo config to show some of the most important commands more easily.
# This is meant to be changed and configured, as it is intentionally kept sparse.
# For a more advanced configuration example see my dotfiles:
# https://github.com/FelixKratz/dotfiles

ITEMS_DIR=$CONFIG_DIR/items

HELPER=git.felix.helper
killall helper
(cd $CONFIG_DIR/helper && make)
$CONFIG_DIR/helper/helper $HELPER > /dev/null 2>&1 &

##### Bar Appearance #####
# Configuring the general appearance of the bar, these are only some of the
# options available. For all options see:
# https://felixkratz.github.io/SketchyBar/config/bar
# If you are looking for other colors, see the color picker:
# https://felixkratz.github.io/SketchyBar/config/tricks#color-picker

BAR=(
  height=38
  blur_radius=30
  position=top
  sticky=off
  padding_left=10
  padding_right=10
  notch_width=220
  color=0xff1f2430
)
sketchybar --bar "${BAR[@]}"

##### Changing Defaults #####
# We now change some default values that are applied to all further items
# For a full list of all available item properties see:
# https://felixkratz.github.io/SketchyBar/config/items

DEFAULT=(
  icon.font="Font Awesome 5 Pro:Light:16.0"
  icon.padding_left=4
  icon.padding_right=4
  label.font="Iosevka Fixed:Medium Extended:13.0"
  label.color=0xffDADBC0
  label.padding_left=4
  label.padding_right=4
  padding_left=4
  padding_right=3
)

sketchybar --default "${DEFAULT[@]}"

##### Adding Left Items #####
# We add some regular items to the left side of the bar
# only the properties deviating from the current defaults need to be set

source $ITEMS_DIR/spaces/item.sh

##### Adding Center Items #####
# We add some regular items to the center of the bar
# only the properties deviating from the current defaults need to be set

source $ITEMS_DIR/date/item.sh
source $ITEMS_DIR/clock/item.sh

##### Adding Right Items #####
# In the same way as the left items we can add items to the right side.
# Additional position (e.g. center) are available, see:
# https://felixkratz.github.io/SketchyBar/config/items#adding-items-to-sketchybar

source $ITEMS_DIR/power_off/item.sh
source $ITEMS_DIR/weather/item.sh
source $ITEMS_DIR/volume/item.sh
source $ITEMS_DIR/battery/item.sh
source $ITEMS_DIR/cpu/item.sh

##### Finalizing Setup #####
# The below command is only needed at the end of the initial configuration to
# force all scripts to run the first time, it should never be run in an item script.

sketchybar --update
